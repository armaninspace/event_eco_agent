# Optional live-agent settings.
# The notebooks are written with deterministic fallbacks when credentials are absent.

OPENAI_API_KEY=
OPENAI_MODEL=gpt-5.2

# Optional Azure OpenAI / Foundry settings for Microsoft Agent Framework examples.
AZURE_OPENAI_ENDPOINT=
AZURE_OPENAI_DEPLOYMENT_NAME=
AZURE_OPENAI_API_KEY=
