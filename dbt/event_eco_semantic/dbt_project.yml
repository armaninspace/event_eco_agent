name: event_eco_semantic
version: "1.0.0"
config-version: 2

profile: event_eco_semantic

model-paths: ["models"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]

models:
  event_eco_semantic:
    staging:
      +materialized: view
    marts:
      +materialized: table
